# OpenAI-compatible (default). Also works with proxies and OpenAI-compatible Grok endpoints.
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4.1

# Native xAI (optional). Enables web search, X search, and code execution.
XAI_API_KEY=
XAI_MODEL=grok-4.6

